#简单编译器
##支持代码示例
```
// This is a comment.
int integer;
int count;
count = 10;
output(\"Hello! Please enter an integer: \");
input(integer);
if(integer > 100)
	tinteger = integer + 1;
else\n" +
	tinteger = integer - 1;
while(count > 0)
{
	integer = integer + 2;
	count = count - 1;
}
output(\"The result is: \");
output(integer);
output(\".\\n\");
```